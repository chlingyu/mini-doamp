apiVersion: v1
kind: Service
metadata:
  name: {{ include "mini-doamp.fullname" . }}
  labels:
    {{- include "mini-doamp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mini-doamp.selectorLabels" . | nindent 4 }}
