<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.minidoamp.core.mapper.WarnIndexMapper">

    <select id="selectTypeSummary" resultType="com.demo.minidoamp.api.vo.WarnIndexTypeSummaryVO" databaseId="mysql">
        SELECT index_type,
               COUNT(*) AS index_count,
               GROUP_CONCAT(index_name ORDER BY index_name SEPARATOR ', ') AS index_names
        FROM t_warn_index
        WHERE deleted = 0
        GROUP BY index_type
        ORDER BY index_type ASC
    </select>

</mapper>
